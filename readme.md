# Revinate Styleguide

### Setup

1. Run `npm install`
2. Run `bower install`

### Update Styleguide

1. Change `styleguide-markdown.md` as needed
2. Run `grunt`

### Compile Sass

1. `cd assets/`
2. `compass compile` or `compass watch`